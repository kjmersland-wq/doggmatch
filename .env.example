# DoggMatch — environment variable names only. Never commit real values.

# Lovable Cloud (Supabase) — client-side, publishable
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=

# Lovable Cloud (Supabase) — server-side
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Lovable platform / connector gateway
LOVABLE_API_KEY=

# Gmail connector (contact form delivery via the connector gateway)
GOOGLE_MAIL_API_KEY=

# Where contact-form messages are delivered
CONTACT_EMAIL=